import 'package:flutter/material.dart';

class HomeScreenDesktopLayout extends StatelessWidget {
  const HomeScreenDesktopLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('desktop');
  }
}
