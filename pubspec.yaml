name: portfolio
description: Zvonimir Babić's porfolio website

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.6.5
  get_storage: ^2.0.3
  dio: ^4.0.6
  logger: ^1.1.0
  flutter_screenutil: ^5.5.3+2
  google_fonts: ^2.1.0
  flutter_spinkit: ^5.1.0
  day_night_switcher: ^0.2.0+1
  animated_theme_switcher: ^2.0.6
  url_launcher: ^6.1.3
  flutter_svg: ^1.0.3
  visibility_detector: ^0.3.2
  intl_phone_field: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/images/

